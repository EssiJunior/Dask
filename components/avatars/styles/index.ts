import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    flexDirection: "row",
    alignItems: "center",
  },

  avatarItem: {
    position: "absolute",
    top: 0,
  }
})

export default styles;